import heroImage from '../../public/hero-image.jpg';

function Hero() {
  return (
    <section 
      id="home"
      className="mt-24 relative max-container">
      <div className="absolute w-full h-[50rem] right-5 bg-contain bg-no-repeat bg-hero bg-right"
      >
      </div>
      
    </section>
  )
}

export default Hero